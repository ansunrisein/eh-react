import {defineMessages} from 'react-intl'

export const texts = defineMessages({
  loginWith: {
    id: 'features.auth-with-firebase.login-button',
    defaultMessage: 'Login with <icon></icon>',
  },
})
